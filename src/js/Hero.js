import React, {Component} from 'react';
import Banner from 'Banner';

class Hero extends Component {
    render{
        return(
            <div className="hero">
                <Banner />
            </div>
        )
    }
}

export default Hero;